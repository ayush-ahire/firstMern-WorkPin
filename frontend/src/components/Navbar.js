import React from "react";
import { Link } from "react-router-dom";

const Navbar = () => {
  return(
    <header className="nav">
        <Link to="/" >Home</Link>
    </header>
  )
};

export default Navbar;
